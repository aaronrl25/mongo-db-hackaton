export type Preference={id:string;category:string;label:string;value:string;confidence:number;scope:'global'|'project';projectId?:string;source:string;evidence:string;updatedAt:string};
export type Memory={id:string;projectId:string;text:string;kind:string;createdAt:string};
export type Message={id:string;role:'user'|'assistant';content:string;createdAt:string;preferences?:string[]};
async function req<T>(url:string,init?:RequestInit):Promise<T>{const r=await fetch(url,{...init,headers:{'Content-Type':'application/json',...init?.headers}});if(!r.ok)throw new Error((await r.json().catch(()=>({}))).error||'Request failed');return r.status===204?undefined as T:r.json()}
export const api={status:()=>req<{mongo:boolean;openai:boolean;elevenlabs:boolean}>('/api/status'),preferences:()=>req<Preference[]>('/api/preferences'),memories:()=>req<Memory[]>('/api/memories?projectId=orbit'),messages:()=>req<Message[]>('/api/messages'),chat:(prompt:string,instructions:string)=>req<{message:Message;applied:Preference[]}>('/api/chat',{method:'POST',body:JSON.stringify({prompt,instructions,projectId:'orbit'})}),addPreference:(p:Omit<Preference,'id'|'updatedAt'>)=>req<Preference>('/api/preferences',{method:'POST',body:JSON.stringify(p)}),deletePreference:(id:string)=>req<void>('/api/preferences/'+id,{method:'DELETE'}),addMemory:(text:string,kind:string)=>req<Memory>('/api/memories',{method:'POST',body:JSON.stringify({text,kind,projectId:'orbit'})})};
